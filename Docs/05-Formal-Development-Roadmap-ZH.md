# 《废土移动城市》正式版后续制作计划

> 路线图版本：v1.31<br>
> 基线日期：2026-08-28
> 引擎：Unity `2022.3.62f1`（修订号 `4af31df58517`）
> 目标平台：Windows 10/11 64 位
> 当前形态：内部候选开发版，不公开发布
> 功能基线：`IDEA-0011` 至 `IDEA-0017`、`IDEA-0019` 至 `IDEA-0023` 均已实现待验证。历史阶段自动化和构建事实不替代尚未完成的用户试玩或真实 Windows 10/11 验收。当前现役正式 3D 使用 schema `34`，现有 `64×48` v2 地图、seed `8128` 与 24 节点保持不变；除 Lv.2/F2 内容外，已接入一个稳定小队、四类单位、远征、一次城/一前哨/实体运输、三角色救援/死亡/继承与两势力外交。`IDEA-0023` 精确验证提交 `0c94c672eab30f926a06675aff413c4c7a3541cf` 的 EditMode `2927/2927`、PlayMode `101/101`、项目质量门、文档校验和三项现役构建均已通过；20 张透明图、140 项统一目录、七 Atlas、公告板和 M/N/P 视觉层已实现，用户视觉/玩法与真实 Windows 仍待收口。

---

## 1. 文档定位

本文件记录正式版持续开发的真实基线与后续顺序。主 GDD `01-Game-Design-Document-ZH.md` 提供产品和系统目标；本文件负责把目标拆成可独立验收、可提交、可回归的开发里程碑。

本版同步已批准需求：

- `IDEA-0001`：基础场景、内外城建造、生产物流、交互与俯视 3D 目标；
- `IDEA-0002`：领袖倒地、救援、永久死亡与继承；
- `IDEA-0003`：3D 建造选择、双网格放置、撤离处理与开发修改器，批准规格为 `Docs/superpowers/specs/2026-08-05-3d-building-placement-and-developer-modifier-design.md`；运行时、验证工具和 Windows 构建门已实现，等待用户验证。
- `IDEA-0004`：第一版正式美术样板包与地形运行时接入；
- `IDEA-0005`：研究站可在移动城市内城建造并携带，未来研究在堡垒态按 100%、其他城市状态按 50% 连续推进；
- `IDEA-0006`：当前 3D 试玩地图扩展为 `64×48`，中央保留原 `32×24` 内容，外围只增加可通行普通荒地。
- `IDEA-0007` 至 `IDEA-0010`：退出与设置菜单、外城可建范围显示、外城住房和采矿站合法位置高亮；自动化、构建及 2026-08-14 用户人工试玩均已完成并通过。
- `IDEA-0011`：生产与物流可观察化、30 格独立背包、两条低效率应急手工合成、Demo 核心科技树运行时与文明式资源状态栏；当前分支已完成运行时与正式 3D 真实输入接线，日常完整 EditMode `1616/1616`、完整 PlayMode `103/103`、项目质量门、四个正式构建和文档生成/校验均已通过，状态为“已实现待验证”。真实 Windows 机器和用户人工试玩仍待完成；当前阶段不得扩展到敌人、炮塔、弹丸或正式存档迁移。
- `BUG-0006`、`IDEA-0012`：用户在 2026-08-18 的首次 `IDEA-0011` Player 试玩中确认建筑旋转/预览问题，并批准真实仓库共享库存与过滤、石料节点、统一资源图标和逐项缺料反馈；当前为“已实现待验证”，先于敌人和防御开发闭环，用户试玩与真实 Windows 验收仍待完成。
- `IDEA-0013`：首个 3D 防御闭环；自动防御改为可研究，加固结构与遗产解析继续预览，接入一座耗弹机枪塔、一种啃噬者、首塔触发的教学波和城市核心受击；当前为“已实现待验证”，不得描述为用户试玩或真实 Windows 验收已经通过。
- `IDEA-0014`：正式撤离与完整 3D 垂直切片；对齐 `5/8` 秒展开收起、转换取消、遇袭收起 `-30%`、战斗完整拆除 `60% + 5 秒`、物资与退款原子容量门和撤离可观察性，并以真实输入闭合“移动→展开→建造→生产→防御→撤离”。本里程碑保持 schema `30`，正式存档适配继续单独审批。
- `IDEA-0015`：正式 3D 存档与 schema `31`；独立 3D payload、统一 Store/Coordinator、单槽开始/继续/保存并退出、事件驱动自动检查点、安全 `.tmp`/`.bak` 文件事务和中文错误反馈已经实现。真实输入已覆盖 Deploying、Packing、八敌战斗、已确认撤离处理中和容量阻塞撤离五类往返；schema `1–30` 仍明确保持旧 2D 身份。当前状态为“已实现待验证”；实现 HEAD `a8f30af` 的日常 EditMode `2069/2069`、完整 PlayMode `127/127`、项目质量门、Windows Release 3D、Windows Development 3D、legacy 2D、macOS universal 3D、官方文档生成/验证和 `RecordVerification` 均已通过，这四项是 Task 14 前的正式构建证据。Task 14 已退役 `FormalPrototype`、47 个 2D 专属运行时脚本（共 51 个 `MonoBehaviour` 类型）和 legacy build 入口；当前只保留正式 3D 场景及 Windows Release 3D、Windows Development 3D、macOS universal 3D 三类构建，退役后的聚焦 EditMode `317/317`、日常完整 EditMode `2039/2039`、完整 PlayMode `77/77`、项目质量门、三项构建、官方文档生成/校验和正式验证记录均已通过。人工试玩与真实 Windows 10/11 视觉、GPU、显存、内存验收尚未完成。共享规则、schema `1–30` decoder、迁移与回归样本必须长期保留。
- `IDEA-0016`：四路线资源、配方、43 节点正式 3D 科技树、开发修改器中文检索与六类透明视觉图批次；F1H 已按“统一目录与 TDD → 资源配方闭环 → 43 节点底向上树 → 修改器中文双搜索 → 正式图分批接入 → 完整回归与三构建”的顺序完成实现。六类代表性样图已经逐类完成并由用户选定：物品 `1`、UI `1`、科技 `1`、建筑 `3`、人物 `1`、世界标记 `3`；每类 `5` 张仍只是代表性样图，不是每项做 `5` 版，旧“10 张通用透明样图”方案及其产物废弃且不进入仓库。实现提交 `41de498031c3eaea086360c55693710defb74cb8` 已完成 31 种资源、30 条配方、43 节点研究、自下向上科技树、多配方生产、资源发现、完整合成/资源观察、Development 修改器、六类 114 项正式透明图、统一视觉目录、30 条配方视觉投影和六类 SpriteAtlas；完整 EditMode `2187/2187`、完整 PlayMode `84/84`、项目质量门、三项现役构建和正式验证记录均已通过。地图和 schema `31` 保持不变；逐项视觉人工验收与真实 Windows 复验仍未完成。
- `IDEA-0017`：单城市十波防御战役；三塔、三敌、建筑受击、统一暂停/`1×`/`2×`、胜负结算、统计和 schema `32` 兼容迁移已经接入正式 3D。当前状态为“已实现待验证/完整自动化与三项构建通过待人工”：唯一终局 revision 驱动结算，胜利继续沙盒，失败读取独立内部最近波前或返回标题；schema `31` 迁移明确标记部分统计。不得把自动化和跨平台构建写成用户试玩、真实 Windows 或最终候选版验收已经完成。
- `IDEA-0019`：现役 `64×48` 正式世界已升级为覆盖全图的 v2 确定性宏区与 24 个固定资源节点；地图尺度、矿点标签和建筑表现比例已接入。聚焦、当时完整自动化与三项构建证据已经记录，但用户对地形/矿区/标签/比例的最终视觉试玩和真实 Windows GPU/显存/内存验收仍未完成。
- `IDEA-0020`：正式 3D 关注度、固定三命轨、压力/Boss、文明首次升阶和 schema `33`。正式研究目录从历史 `43` 墠为 `44` 节点并开放遗产解析；三命轨实现 Lv.1/Lv.2，Pressure 与十波串行，晶壳母体复用正式 Defense，玩家可按 `U` 完成文明 `1→2`，升级 HUD 已接入选中建筑，Development 修改器提供中文动作列表/搜索。当前状态为“已实现待验证”；日常完整 EditMode 2803 项、完整 PlayMode 96 项、质量门和三项现役构建均已通过，人工试玩与真实 Windows 仍未完成。
- `IDEA-0021`：文明 Lv.2 两条自然研究/原位升级、F2 六桥、5 新建筑、3 新配方、EMP/血肉灵丹、35 建筑透明图与树枝科技树 UI 已实现。准确提交 `8501e7d9ed03cf0137c979ead0763f6e5222eec7` 的 EditMode `2829/2829`、PlayMode `99/99`、三项构建和正式验证记录已通过；人工视觉与真实 Windows 待验收。F3 单位部署/指挥与新 3D 建模未提前实现。
- `IDEA-0022`：用户批准连续实现 F3 军队/远征、F4A 两城/前哨/实体运输和 F5A 外交/倒地救援/死亡继承/忠诚危机，然后停止。三阶段共用 schema `34` 扩展聚合，保持现有地图、seed、节点和主城深度运行时；不进入文明 Lv.3+、完整 AI 文明、实体内战、跨循环多槽或 F6 候选版。
- `IDEA-0023`：F6 图像可完成子阶段已实现 20 张透明图、140 项统一视觉目录、七 Atlas、单位/地点/运输公告板、三角色头像、M/N/P 正式皮肤、页签与八状态徽记；保持 schema `34`、地图和领域真值不变。真实模型、动画、VFX、SFX、完整设置/无障碍与候选版归档继续留待后续。
- `DOC-0001`：项目测试、可复用目录与自动文档维护规范；工具、主文档和机器验证已完成，等待用户验证。

需求原始确认、状态和边界见 `Docs/06-User-Feedback-and-Change-Control-ZH.md`。

历史 MRV 与 14 天 Demo 计划继续保留，但不再限制正式版后续功能范围。发生冲突时：

1. 已运行代码、自动化测试和 Windows 构建结果代表当前事实；
2. 主 GDD 代表正式版目标；
3. 本路线图代表后续实现顺序；
4. 历史 MRV、Demo 计划和旧进度记录只用于追溯决策。

## 2. 当前完成度

### 2.1 总体估算

- 主 GDD 正式版整体：约 **50%**；
- 单城市核心可玩循环：约 **91%**；
- 正式美术、动画与音频：约 **8%**；七类正式地表与 Ruins/Cliff 首批几何已进入 Unity 运行时，但 Ruins/Cliff 仍待用户视觉复验，城市、领袖、建筑、UI、特效和音频仍主要为程序化占位，因此不因两个有限地形里程碑自动上调总体比例；
- 自动化与 Windows 构建链：约 **90%**。

百分比只用于判断投入方向，不作为发布承诺。每完成一个独立系统后重新审计。

### 2.2 自动化规则基线与正式 3D 状态

本节保留的长期五类敌人、路线炮塔、友军、领袖技能和高等级文明机制仍含共享纯规则与历史自动化，不能一概写成现役玩法。当前正式 3D 已推进到 `IDEA-0020`：三座基础塔、三类十波敌人、关注度 `30/60/90` 压力、晶壳母体、固定三命轨 Lv.1/Lv.2、文明 `1→2`、44 节点研究、建筑升级命令/HUD和 schema `33`；其余敌塔、命轨 Lv.3–9、文明 Lv.3–10 与长期系统仍按实际消费者判断。聚焦自动化不等于本轮完整回归、构建或人工验收。

- 移动城市、相机、地图边界、展开/收起和长周期作业门控；
- 固定世界、探索、迷雾、资源采集和领地缓存；
- 建筑网格、施工、维修、物流、人口容量和路线生产；
- 44 节点四路线研究树、首次文明 `1→2` 与后续时代路线；
- 关注度、压力波次、敌人品质、五类敌人、Boss 多阶段机制；
- 炮塔、路线防御建筑、心灵控制、傀儡与巨兽友军；
- 领袖救援、主动技能、基因剪接光环和意识网络救援；
- 命轨、引导、统计、标题流程、失败与继续；
- 统一友军目标选择、固定集结点、随城回归、死亡统计和组织再生；
- 血肉路线感染闭环：生物伤害叠层、持续伤害、满层范围传播、精神控制清除和程序化占位反馈；
- 科技路线过载与无人系统：2×攻速、能量伤害 +30%、过热停火、岑烬增益组合、侦查无人机和自动维修机甲占位反馈；
- 修仙路线剑意、御剑台与傀儡维护：飞剑命中叠层、20 层真实斩杀、御剑术射程升级、能晶断供休眠/补给恢复和程序化占位反馈；
- 灵能路线共鸣与集体意识单城效果：5 秒共鸣标记、最多 10 个目标、30% 灵能伤害同步、新研究继承 20% 进度和程序化占位反馈；
- 四路线终端建筑：发电站、聚灵阵、代谢炉、意识网络具有人口/研究门槛、物流生产、程序化占位反馈和存档恢复；意识网络在线时启用免费远程救援；
- 终端建筑当前继续使用代理资源：能源币/灵石映射能晶，精神力结晶映射灵能增幅器；正式资源拆分留给后续资源统一里程碑；
- 全部 30 座当前建筑已有位置与运行标签；地面建筑仅在城市稳定展开时施工和运行，内城移动建筑规则、容量效果、生产、防御、维修和单位制造统一服从同一运行状态；
- 移动态支持 WASD 直接驾驶和右键目的地自动寻路，恶劣地形减速、深水/悬崖阻挡；展开前校验 3×3 城市占地，展开态由领袖接管直接控制；
- 镜头默认精确跟随当前直接控制对象；中键反向拖动进入自由状态并在释放后保持，`Home` 同帧返回，控制对象切换时自动恢复跟随；暂停时镜头输入继续可用；
- `808c0d6` 及其 `391/391` EditMode、`50/50` PlayMode 验证结果构成退役前的 2D 功能冻结点；该历史场景和专属回归入口现已由 Task 14 退役，历史证据继续保留；
- 首个“可操作 3D 基础”已在独立 `GrayboxPrototype3D` 场景和 `WasteCity.Graybox3D` 程序集中实现：逻辑二维坐标映射 Unity XZ，seed `8128` 的当前 `64×48` 合批世界将原 `32×24` 内容逐格居中保留并增加 `2304` 格空旷荒地，3D 城市手动驾驶与四方向 A*、地形阻挡/减速、3×3 展开校验与展开/收起、开发夹具领袖直接控制、倾斜正交镜头、场景作用域 URP 和独立 3D Windows 构建均已接通；该阶段不读写正式存档；
- 版本化正式 3D 存档当前为 schema `33`；schema `31→32→33` 保持单向迁移，schema `1–30` 继续保持 legacy 2D 历史身份、decoder、默认规则与固定 fixtures。旧 3D 档迁入清洁 progression 默认值，不从历史建筑或研究反推关注度、命轨或压力经历；
- 2026-08-15 的阶段历史回归为日常 EditMode `1454/1454`、完整 PlayMode `91/91`，均为零失败、零跳过；EditMode 只排除按批准边界该批不运行的 `TerrainAssetDeep`。该结果覆盖当时的正式场景 `64×48` 世界、中央 `768` 格与外围 `2304` 格、研究站真实输入放置与 Ruins/Cliff 分类回退；其后 Task 14 已退役 2D 专属场景、控制器和测试入口，因此这组数字只作为历史证据，不冒充当前 HEAD 的最新全量结果；
- 无界面编译 0 错误；
- 2026-08-15 最终 v8 构建依次通过正式入口：Release 3D `BuildWindowsGraybox3D()` 输出 `Builds/Windows3D/WasteCityGraybox.exe`、Development 3D 输出 `Builds/Windows3DDevelopment/WasteCityGrayboxDev.exe`、legacy 2D 输出 `Builds/Windows2D/WasteCity2D.exe`，三个 Windows 产物均由系统标头工具确认为 `PE32+`、Windows GUI、x86-64；`BuildMacOSGraybox3D()` 输出 `Builds/macOS/WasteCity.app`，精确 Player binary 为 universal `x86_64 arm64`；
- 自动性能门结果为核心世界 Renderer `12`、长期生成对象 `12`、适配器显式 tick 预热后连续 `300` 次托管分配 `0 B`；独立验收五次世界生成耗时为 `10.4615 / 2.5009 / 2.4076 / 2.4465 / 2.4240 ms`，中位数 `2.4465 ms`；
- IDEA-0003 结构性能门使用 seed `8128`、`32×24` 世界真实创建并清理 `128` 个混合建筑（完成/施工/遗迹 `43/43/42`）：实例 Renderer `128`、基础设施 Renderer `2`、长期建筑表现对象 `133`，未创建 `768` 个逐格对象或 `28` 个目录对象；最终五次耗时为 `118.0184 / 78.8799 / 50.2926 / 24.9399 / 28.7919 ms`，中位数 `50.2926 ms`；预热后连续 `300` 次建筑输入/会话/施工/撤离适配调用托管分配 `0 B`；
- IDEA-0004 首个地形运行时样板已接通：七层 BaseColor/Normal/URP Mask/Height Texture2DArray、共享 URP 主材质与 Shader、`384×256` 控制图、世界空间 UV、柔和边界混合、DeepWater 动态参数、单个连续地形 Mesh/Renderer、灰盒回退和增量场景 authoring 均已进入默认 `GrayboxPrototype3D`；地形只负责表现，不改变二维地形规则、碰撞、寻路、存档或资源节点真值；
- IDEA-0004 在 seed `8128`、`96×64` 世界的五次生成耗时为 `297.6568 / 173.9275 / 173.2853 / 186.2036 / 164.6840 ms`，中位数 `173.9275 ms`；正式结构为 `1` 个 Renderer、`1` 个长期表现对象，预热后托管分配 `0 B`。Full HD `1920×1080`、关闭 Deep Profile 的连续 `300` 帧记录为平均 `469.0956 FPS / 2.1318 ms`、最小/最大帧时 `1.1458 / 41.7336 ms`；输入、施工、撤离标记各 `300` 帧且后代 `GC Alloc` 为 `0 B`，地形 presenter 无 `Update/LateUpdate`；四个压缩数组载荷合计 `127,227,779 B`，Editor 观察到的原生内存为 `254,457,350 B`，该数值不是 Windows Player GPU 显存读数，独立 GPU 帧时也未取得；
- Ruins/Cliff 运行时子项已实现：14 个原始 FBX 继续保持 Read/Write 关闭，Ruins `8` 个与 Cliff `6` 个 Prefab 各内嵌唯一可读运行时 Mesh，重建稳定性由 Prefab GUID/Mesh localFileID 测试保护；`FirstArtRuinsCliffCatalog3D`、Profile、确定性布局、两类合批几何、13 个共享材质、分类原子回退和唯一地形 presenter 接线已经完成。AssetBuilder focused EditMode 为 `37/37`，固定证据捕获为 `12/12` 张；最终日常 EditMode `1454/1454`（只排除 `TerrainAssetDeep`）、完整 PlayMode `91/91`。seed `8128`、`64×48` 的布局与合批五次中位数 `59.1255 ms`、地表加几何总初始化五次中位数 `95.8269 ms`，稳定观察 `300` 次托管分配 `0 B`，长期结构为 `2` 个 Renderer、`3` 个对象、`312,914` 顶点、`168,824` 三角形、`13` 个材质槽。GUI Profiler 的连续 `300` 帧平均 `174.083 FPS / 5.7444 ms`，Rendering 观察为 SetPass `19`、Draw Calls/Batches `49/49`。最终 v8 四构建均成功；每次完整退出后 `21` 个 ProjectSettings 与 `14` 个运行时 Prefab 哈希精确稳定，普通/最终退出恢复标记和备份均为 `0`。macOS 精确 binary 的 `45` 秒 NullGfx 启动冒烟中，`31` 条错误全部为无图形设备下预期的 unsupported Shader，脚本异常、空引用、未处理异常、Missing Script 与崩溃均为 `0`；该冒烟不证明真实渲染、GPU 或显存。真实 Windows 10/11 Player 的视觉/GPU/显存冒烟和用户对本次 Ruins/Cliff 运行时画面的视觉复验仍待完成，因此以上只属于自动化与开发机证据，不得记为视觉通过；
- Windows 开发机 Unity GUI Profiler 在 `GrayboxPrototype3D`、Full HD `1920×1080`、关闭 Deep Profile 后记录连续 `300` 帧：平均 `592.8491 FPS / 1.6868 ms`，最小/最大帧时 `1.1465 / 46.0970 ms`；输入、施工、撤离三个可见适配器样本各出现 `300` 次且自身后代 `GC Alloc` 均为 `0 B`，会话施工调用由关闭 Deep Profile 的施工控制器样本包含，并由独立 `300` 次直接调用零分配门补证；原始 `.data`、Game View 和 Profiler 截图只保存在仓库外证据目录；
- 默认编辑/构建入口已由独立提交 `8ce573a` 切换为 `GrayboxPrototype3D`；其后曾保留的 `FormalPrototype` index 1 与 `BuildWindowsLegacy2D()` 现已由 Task 14 退役，当前 Build Settings 只保留正式 3D 场景；
- 上一正式基线完成过独立程序 Direct3D 11 冒烟 12 秒，0 异常、0 错误；
- 上一功能基线的三个 Windows 产物已在真实 Windows 10/11 开发机完成 `1920×1080`、`12` 秒独立运行冒烟；本次 Ruins/Cliff 最终 v8 的三个 Windows 新产物只完成构建成功与 PE 格式确认，macOS universal 产物只完成 NullGfx 启动/关闭冒烟。真实 Windows 10/11 Player 的视觉、GPU、显存和内存复验仍待补，不代表本次用户试玩验收或公开发布批准。

### 2.3 表现资源接口

当前功能默认使用灰盒 Sprite、色块和程序化占位。正式目标是保持同一俯视规则的 3D 场景，不改成第三人称。所有新增单位、建筑、角色和关键特效必须：

- 使用稳定 ID；
- 通过当前正式 3D 的稳定目录、`GrayboxVisualSlot` 和可替换 Prefab/材质配置接入；
- 允许后续用 3D Prefab、动画或 Sprite 替换，不修改玩法逻辑；
- 不把碰撞、生命、攻击或存档状态放入纯美术 Prefab；
- 正式模型/贴图导入前记录来源、许可证和目标 Unity 路径。

### 2.4 2D 冻结点与 3D 迁移决策

- 镜头里程碑完成并通过全量验证后，项目曾冻结 2D 功能基线；该过渡决策在 Task 14 前要求 `FormalPrototype`、规则层、稳定 ID、schema `30` 存档和当时自动化测试全部保留；
- 停止新增仅服务于 2D 场景的交互实现。尚未完成的建造菜单、内外城放置、物流观察、前哨、迷雾、领袖交互和其余 F1A 功能转到 3D 基础场景继续实现，不先制作重复的 2D 版本；
- 3D 迁移审计、设计规格、可执行计划和首个可回退“可操作 3D 基础”均已完成并通过独立验收；Task 14 又在 schema `31` 退役前门通过后移除 2D 专属运行时，保留共享规则与旧存档兼容资料；
- 默认编辑/构建入口已经独立切换到 3D；当前 Build Settings 只启用 `GrayboxPrototype3D`，不再包含 `FormalPrototype` 或 legacy 2D 构建入口；
- 首轮 3D 只使用方块、基础材质和占位 Prefab，不批量制作或导入正式模型、美术、动画与特效；
- 本决策只改变实现顺序，不改变主 GDD 的产品目标、已确认玩法规则或真实 Windows 10/11 冒烟门。

## 3. 后续开发阶段

### 阶段 F1：单城市正式玩法补齐

目标：让四条路线在单城市循环内都有完整生产、防御和单位选择。

1. 统一友军单位规则：目标选择、集结点、脱战跟随、死亡统计和组织再生（已实现：`d5baf17`；本基线 Windows 冒烟已补）；
2. 科技路线无人机与过载机制（已实现：`fd42357`；本基线 Windows 冒烟已补，正式无人机美术待后续）；
3. 血肉路线感染/寄生状态闭环（已实现：`9792fcb`；本基线 Windows 冒烟已补，正式巨兽美术反馈待后续）；
4. 修仙路线剑意、御剑台升级与傀儡维护（已实现：`6200703`；本基线 Windows 冒烟已补，正式美术待后续）；
5. 灵能路线精神灼烧、集体意识的单城等价效果（已实现：`95abb35`；本基线 Windows 冒烟已补，正式美术待后续）；
6. 发电站、代谢炉、意识网络等缺失路线建筑（已实现：`2e13d6e`；本基线 Windows 冒烟已补，正式资源拆分和美术待后续）；
7. 四路线资源、建筑和研究说明统一显示（已实现：`970ad60`；本基线 Windows 冒烟已补）。

验收门：每个功能先写规则测试；全部 EditMode/PlayMode、编译、Windows 构建和冒烟通过；每个里程碑独立提交。

### 阶段 F1A：单城市场景与交互基础

目标：保留已经验证的 2D 规则与交互基础，把未完成的单城市移动、内外城建造、生产、物流、角色与前哨交互作为 F1B 3D 灰盒场景的后续功能要求，不再先制作一套 2D 专属实现。

- 该阶段当时的 2D 功能基线冻结于镜头里程碑 `808c0d6`；`FormalPrototype`、规则层、稳定 ID、存档与自动化测试在 Task 14 前作为强制回归基线，现仅保留共享纯规则、稳定 ID 与 schema `1–30` 兼容回归；
- 明确移动态和展开态：移动中允许内城轻型施工、研究站携带与维持，地面施工和重型工业需要展开；研究运行时在堡垒态按 100%、移动/展开中/收起中按 50% 连续推进。该能力在 F1A 当时未实现，现已由 `IDEA-0011` 接入正式 3D 运行时并处于“已实现待验证”；
- 移动态默认控制城市、展开态默认控制领袖；补齐目的地自动寻路、基础地形阻挡和展开位置合法性（规则与 2D 交互基础已实现：`8693086` 至 `11c90b9`；镜头跟随、自由拖动与快速返回已实现：`9bbcca5`、`118b3f0`、`808c0d6`；本基线 Windows 冒烟已补，自动驾驶科技升级和正式表现待后续）；
- 建立内城、地面、两者皆可的建筑位置标签，以及移动可运行、仅展开运行、地形依赖等运行标签（规则基础已实现：`60b891e`；IDEA-0003 已完成内城放置交互并通过本基线 Windows 冒烟，前哨待后续）；
- 建造面板提供快捷栏、分类、搜索、置灰原因和可见性规则；隐藏、未接触路线和唯一特殊建筑不进入普通列表；
- 放置交互支持预览、合法性原因、旋转、连续放置、拖动城墙和取消规划；
- 保留城市范围内自动物流，补齐产销、缺口、阻塞、优先级和请求库存的可观察反馈；
- 建筑脱离城市范围后进入前哨状态，保留库存并显示补给、维护和通信问题；提供撤离清单与基础受袭警报；
- 迷雾采用未探索隐藏、已探索变暗、当前视野实时更新、最后情报可过期的规则；
- 领袖允许低效率手工采集以处理开局或紧急缺口，但不得替代自动采矿闭环；
- 镜头默认跟随当前控制对象，支持自由拖动和快速返回（2D 交互已实现：`9bbcca5`、`118b3f0`、`808c0d6`）；暂停允许规划和下令但不推进规则时间；
- 领袖基础行为可由 AI 执行，玩家可主动接管；Demo 继续使用抽象城市生产力，不生成实体施工工人；
- `BUG-0001` 已明确为冻结 2D `FormalPrototype` 的历史问题，用户决定不修复，状态为“已明确 / 已批准 / 不适用”；它没有修复提交，新的 3D 建造功能不得被计作该问题的修复。

验收门：现有 2D 功能以 `808c0d6` 和全量自动化结果冻结，不再以“完成全部 2D F1A 交互”作为进入 F1B 的前置条件。3D 迁移审计、设计、实施计划、首个可操作基础和 IDEA-0003 建造灰盒已经通过独立机器验收；后续变更仍必须持续通过 2D 规则测试、PlayMode 主循环和旧存档回归。真实 Windows 10/11 三目标独立程序冒烟已补，但用户试玩验证和候选发布批准仍未完成。

### 阶段 F1B：俯视 3D 垂直切片

目标：在不重做核心玩法的前提下，立即开始可回退的俯视 3D 灰盒迁移，并在新的 3D 基础场景中继续实现尚未完成的 F1A 交互。

- 3D 迁移审计、设计规格和可执行计划已完成；批准规格为 `d3c10ec`，实施计划基线为 `52c4637`；
- 独立、可关闭或移除的 `GrayboxPrototype3D` 已建立；该阶段当时仍保留 2D 场景、规则网格、稳定 ID、schema `30` 存档模块、逻辑坐标和全部自动化测试。Task 14 现已退役场景专属内容，只保留共享纯规则、稳定 ID、逻辑坐标与 schema `1–30` 兼容资料；
- 第一阶段已使用 Unity 原生 Plane、Cube、Capsule、基础 URP 材质和程序化占位建立地形、移动城市、领袖、直接控制与镜头基础；IDEA-0003 又完成内城平台、28 项建造目录、双网格放置、施工、撤离处理和开发验证入口；`IDEA-0011` 已进一步接入生产/物流观察及关联界面；敌人与防御在该历史里程碑中仍排除，现由已实现待验证的 `IDEA-0013` 独立承接最小闭环；
- 在 3D 基础场景中继续实现前哨、迷雾、领袖交互和其余未完成 F1A 功能，不先补一套 2D 版本；生产/物流观察由 `IDEA-0011` 保持“已实现待验证”；
- `IDEA-0003` 独立 3D 建造适配层已实现：普通目录只接 `BuildingCatalog.BuildMenu` 28 项，支持底部快捷栏与完整目录、旋转、连续放置、外城半径 8 世界网格、随城移动的 `8×6` 内城网格、施工与确定性退款、收起前遗弃/完整拆除/快速拆除清单，以及仅 Editor/Development Build 可用的开发修改器；实现提交范围为 `6ee4712` 至 `131dbb1`，验证与 Development 构建提交为 `9d2ea53`；规格见 `Docs/superpowers/specs/2026-08-05-3d-building-placement-and-developer-modifier-design.md`；
- `IDEA-0004` 已完成七类连续地表和 Ruins/Cliff 两个有限运行时样板：七类地表共 `28` 张 `2048×2048` PNG 已构建为四个七层 Texture2DArray，并通过共享 Material/Shader、控制图和单个连续 Mesh 接入默认 3D 场景；Ruins `8` 件与 Cliff `6` 件则通过独立 Catalog/Profile、14 个内嵌可读 Mesh 的 Prefab、13 个共享材质、两类确定性合批和分类原子回退接入同一 presenter，未扩展冻结 2D `VisualLibrary`。Ruins/Cliff 自动化与证据捕获已通过，但运行时结果仍待用户视觉复验；城市、领袖、建筑、UI、VFX 和 SFX 也不在本里程碑内。连续地表严格视觉门仍未通过：DeepWater 蓝黑色比、地形类别区分和水面动态可感知度低于目标；用户于 2026-08-11 选择接受当前第一版颜色/分类偏差，并另选 `B` 接受“深水动态不足”作为已知问题，后续可仅替换材质、Shader 或数组内容继续修正，不得写成视觉目标已完成；
- `IDEA-0005` 的研究站移动内城建造子范围与 `IDEA-0006` 的 `64×48` 稀疏地图已经实现：研究站现为 `Either + MobileAllowed`，正式 3D 场景可在 Mobile 内城通过真实输入创建施工实例；原 `32×24` seed 世界按 `(16,12)` 偏移逐格居中，全部旧内容世界位置不变，外围 `2304` 格固定为无资源的 Wasteland/Open；研究菜单、研究推进和 `100%/50%` 运行时在该里程碑当时排除，现转入 `IDEA-0011`；完整 `96×64` 布局和区块流送继续排除；
- `IDEA-0007` 至 `IDEA-0010` 3D 易用性跟进包已完成并于 2026-08-14 通过用户人工试玩：外城住房继续复用统一规则，外城覆盖层读取统一范围真值，采矿高亮读取统一放置评估，模态退出/设置菜单遵守既有取消链；冻结 2D 场景和 schema `30` 均未修改；
- 住房当前在移动形态仅允许内城、要塞形态允许内外城，内外城数值暂时相同；“外城住房生命值更低、人口容量更少”只登记为未来平衡方向，必须另行完成数值设计与批准；
- 相机当前采用倾斜正交俯视跟随、自由拖动和 `Home` 返回，不进入第三人称；缩放不属于首个基础里程碑，留待后续独立评审；
- 玩法真值留在规则与运行时组件，模型、动画、材质和特效不得持有生产、生命、攻击或存档真值；
- 首个垂直切片固定为“移动→展开→建造→生产→防御→撤离”；
- 优先验证地形、移动城市、内城平台、领袖、基础建筑、一种敌人、镜头、选择、网格、遮挡、性能和 Windows 构建；建造 Task 13 通过后只允许进入 `IDEA-0004` 的有限美术样板验证，完整垂直切片通过后才允许批量制作正式美术。

验收门：3D 迁移审计、设计、实施计划与“移动、展开、领袖、镜头、建造、性能、独立 Windows 构建”灰盒门已通过，默认编辑/构建入口仍为 3D；退役前 2D 基线与 schema `30` 回归持续通过。生产、物流与关联观察界面已由 `IDEA-0011` 实现，完整自动化、项目质量门、四个正式构建和本机 macOS Player 启动冒烟已经通过，仍等待真实 Windows 机器和用户人工复验；`IDEA-0013` 的首个战斗与防御闭环已完成自动化、项目质量门、四个正式构建和本机 macOS Metal 启动冒烟，状态为“已实现待验证”。`IDEA-0014` / F1F 已完成正式撤离、六段真实输入垂直切片、混合性能门、日常完整 EditMode `1787/1787`、完整 PlayMode `112/112`、项目质量门、四个正式构建和 `RecordVerification`，状态仍为“已实现待验证”，因为用户试玩与真实 Windows 验收尚未完成。`IDEA-0015` 的正式 3D 存档与 schema `31` 当前同样为“已实现待验证”；实现 HEAD `a8f30af` 已通过日常 EditMode `2069/2069`、完整 PlayMode `127/127`、项目质量门、四个退役前构建、官方文档生成/验证和 `RecordVerification`。Task 14 已完成 2D 专属入口退役，退役后的聚焦 EditMode `317/317`、日常完整 EditMode `2039/2039`、完整 PlayMode `77/77`、项目质量门、三项现役 3D 构建、文档生成/校验和正式验证记录均已通过；人工试玩与真实 Windows 10/11 视觉、GPU、显存、内存验收仍未完成。连续地表和 Ruins/Cliff 已具有正式场景运行时接线、稳定映射和灰盒回退；Ruins/Cliff 仍须用户视觉复验，真实 Windows 10/11 Player 的 GPU、显存和视觉冒烟也仍待补，不能写成整个第一版美术包已完成。

### 阶段 F1C：IDEA-0011 生产、物流与可观察化

状态：**已实现待验证**。当前分支的运行时、场景 UI 与真实输入接线已经完成；日常完整 EditMode `1616/1616`、完整 PlayMode `103/103`、项目质量门、Windows Release 3D、Windows Development 3D、legacy 2D、macOS universal 3D 构建和文档生成/校验均已通过，`RecordVerification` 在最终实现提交上记录证据。真实 Windows 10/11 机器的视觉、GPU、显存、内存复验和用户人工试玩均未完成。

目标：在正式 3D 输入/UI 主循环中完成机枪塔之前的生产闭环，并让资源从节点、建筑内部库存、城市物流库存到玩家背包的流向可观察、可解释、可安全操作。

首批范围：

1. 生产与物流规则：复用 `BuildingCatalog`、`WorldMapModel`、统一放置/施工/撤离系统、城市物流范围和既有资源账本；实现铁矿/能晶节点按类型采出对应资源、采矿 `1 单位/3 秒`、冶炼 `2 铁矿→1 合金/6 秒`、装配 `2 合金→2 弹药/6 秒`。机器配方以 `core.production.extract-node-resource`、`core.production.smelt-alloy`、`core.production.assemble-ammunition` 进入统一正式资源配方目录；`3/6/6` 秒为最终有效周期，不叠加冻结 2D 要塞生产倍率。物流步先卸旧输出、补内部输入，再推进只消费内部缓存的生产，本步新输出等下一物流步卸载；采矿只在周期完成时调用既有 `Harvest`；同时实现 A16.3 的内部容量、资源点枯竭和脱离/重连物流；本阶段实现时使用的“仓库每资源 `+150`”模型已由后续 `IDEA-0012` 批准规则替代，保留在此只作为历史实现事实；
2. 运行状态：统一公开缺少输入、输出已满、不在物流范围、矿脉已枯竭、玩家暂停运行五类首要原因；战术/系统暂停只冻结规则时间，不覆写建筑手动状态；
3. 玩家背包：独立于城市和建筑库存的 `10×3`、共 30 格容器；首轮每资源每格上限 100，但必须由正式资源配置提供；支持左键拿取/合并/交换、右键拆半/逐个放置和 `Shift+左键` 原子快速转移；自动生产不直接消费背包；
4. 人工访问：当前直接控制的城市或已招募领袖距建筑 footprint 边缘不大于 2 格时才能转移该建筑库存；城市库存则在控制城市时始终可访问，控制领袖时仅在距核心不大于 2 格时可访问；转移先计算目标实际可接收量，再只对该数量执行原子事务，允许容量有限时部分接收；
5. 应急手工合成：单工作槽 FIFO、队列总上限 20，只消费和产出背包资源；完整稳定 ID 为 `core.crafting.field-alloy`（`4 铁矿→1 合金/12 秒`）与 `core.crafting.field-ammunition`（`4 合金→2 弹药/12 秒`），分别复用 `core.research.automated-machinery` 与 `core.research.precision-assembly` 作为前置；左键排 1 次、右键必须完整排 5 次否则拒绝、`Shift+左键` 排输入与队列余量允许的最大次数；入队只预留输入，完成时输出空间不足则阻塞；不制作建筑物品、工作台、并行槽或建筑配方切换；
6. 首版科技树：在 3D Demo release profile 投影六个可见核心节点；废料加工已解锁，仅基础冶金与弹药装配可研究，自动防御、加固结构、遗产解析锁定预览；复用四个既有 ID，并批准新增仅属于六节点语义的 `core.research.scrap-processing` 与 `core.research.reinforced-structures`，不得为已有语义新增平行节点。`core.research.precision-assembly` 在该投影显示“弹药装配”，冻结 2D/长期 43 节点目录继续保留原显示“精密装配”；研究启动和推进都要求合格研究站，失去最后一座时暂停不取消；研究站效率执行堡垒 `100%`、其他城市状态 `50%`；取消研究完整返还 80%，退款空间不足则拒绝取消；
7. 文明式资源状态栏：铁矿、能晶、石料、生物质、水五种基础资源常驻；合金和弹药在发现或解锁后动态加入；显示图标、当前量/有效容量和近期净变化，悬停显示收入/消耗、耗尽或装满估计与容量来源，点击打开只读账本；路线资源在接触或解锁前隐藏；
8. 输入与 UI：`E` 打开背包/合成，`T` 打开科技树；系统菜单 > 背包/科技 > 建造 > 世界的模态优先级必须阻止输入穿透，UI 测试必须走真实输入主循环；
9. 状态与存档：本里程碑所有新增 3D 状态先保持会话态，退出继续明确提示不保存；schema 固定为 `30`，不得借用旧字段或提前制作正式迁移。

自动配方与应急手工配方共用同一正式配方目录，但使用不同稳定 ID 和数值条目。明确排除：电力、传送带、具体工人、相邻八格共享、跨城市运输、新资源 ID、敌人、炮塔、弹丸、建筑物品制作、正式存档迁移和冻结 2D 功能接线。

准确基线 `BuildingCatalog` 中冶炼厂和研究站的施工成本与 GDD A16.2 仍有既存差异；F1C 不静默调整这两项施工成本，后续按独立变更控制处理。科技完成关注度运行时也不在 F1C 生效范围。

当前实现结果：正式 3D 会话已经接入三条机器配方、建筑内部缓存、城市自动物流、脱网本地运行、仓库容量、节点枯竭、五类停工原因与单建筑暂停；`E` 面板接入 30 格背包、城市/建筑缓存原子转移和两条应急合成 FIFO；`T` 面板接入六节点投影、两项可研究科技、三项锁定预览、研究站资格、堡垒/其他状态 `100%/50%` 进度与取消退款；顶部资源栏和 15 资源账本读取同一正式库存，并通过正式 Input System 主循环与建造、世界和系统菜单协调。schema 仍为 `30`，冻结 2D 没有新增功能接线。

正式自然开局曾有一个 bootstrap 边界：GDD A6.1 的正式城市库存把石料初始化为 `0`，而准确 `BuildingCatalog` 中冶炼厂施工仍需 `6` 石料。F1C 没有静默改变任一数值，也没有把开发夹具的石料当作正式开局资源；自动生产链测试只通过显式开发补给搭建 `2 采矿站 → 2 冶炼厂 → 1 装配厂`，随后清零铁矿、合金和弹药等生产物资，再验证节点与机器自动生成整条链。该测试证明运行链，但当时未证明自然开局具备石料获取路径；该边界随后已由 `IDEA-0012` 以 seed `8128` 原始内容区的可采石料节点实现并通过自动化，仍等待用户试玩和真实 Windows 视觉复验。

收尾门：保持已经完成的 TDD 实现，继续运行聚焦测试、日常完整 EditMode、完整 PlayMode、独立静态审查、正式构建、官方文档生成与 `RecordVerification`。`TerrainAssetDeep` 仅在触发其既定高风险条件或准备发布时运行。自动化和构建通过不得写成用户试玩或真实 Windows 视觉/GPU/显存/内存验收已经完成。

### 阶段 F1D：BUG-0006 与 IDEA-0012 建造、仓储和资源可读性跟进

状态：**已实现待验证**。四向旋转与透明预览、每仓库 `150` 共享真实库存与单资源过滤、石料采集、15 资源共享图标和逐项缺料反馈已接入正式 3D；日常完整 EditMode `1656/1656`、完整 PlayMode `105/105`、项目质量门和四个正式构建均通过。macOS universal 产物仅完成 NullGfx 脚本/崩溃冒烟；用户人工试玩与真实 Windows 10/11 视觉、GPU、显存、内存验收仍待完成。

目标：修复矩形建筑旋转与朝向预览；把仓库从全局容量倍率改成可查看、可过滤的真实 `150` 共享容器；保证自然开局可发现并采集石料；让全部资源在世界与 UI 中使用统一图标；让放置失败准确列出材料缺口。

实施顺序：

1. `BUG-0006`：用正式输入固定旋转 RED，统一锚点、模型、footprint、箭头和合法性刷新；
2. 仓库规则：建立每实例会话态共享容器、过滤、聚合与原子路由，再接仓库详情和玩家操作；
3. 资源节点与图标：由 `WorldMapModel` 保证 seed `8128` 原始内容区石料节点，采矿兼容铁/能晶/石料；建立 15 资源稳定图标目录并接世界浮标及全部材料 UI；
4. 精确缺料：统一规则层生成逐项缺口，预览与点击确认共用结果；
5. 完成聚焦测试、日常完整 EditMode、完整 PlayMode、质量目录、正式构建、macOS Player 冒烟、文档生成与 `RecordVerification`。schema 保持 `30`，冻结 2D 只做回归，不进入敌人、炮塔、弹丸或存档迁移。

### 阶段 F1E：IDEA-0013 首个 3D 防御闭环

状态：**已实现待验证**。规则层、正式 3D 场景/UI、真实输入和池化表现已接通；日常完整 EditMode `1709/1709`、完整 PlayMode `109/109`、项目质量门和四个正式构建均通过，macOS universal Player 已在本机 Apple M4 Metal 设备启动约 `12` 秒且未发现脚本、Missing Script、Shader 或崩溃错误。真实 Windows 10/11 视觉/GPU/显存/内存复验与用户人工试玩仍未完成。

目标：把已经实现待验证的生产、物流、背包、科技树和资源观察链连接到首个弹药消费者，以最小的一塔、一敌、一教学波验证“生产 → 研究 → 建造 → 补给 → 防御 → 城市受击”闭环，并为后续战斗扩展建立可测试的规则/表现分层。

实施顺序：

1. 先以失败测试固定正式配置和科技投影：自动防御使用既有稳定 ID、前置弹药装配、`12` 合金 + `10` 生物质、`35` 秒并改为 `Researchable`；加固结构和遗产解析仍为 `PreviewOnly`；schema 保持 `30`；
2. 建立独立规则时钟和可测试战斗状态：城市核心 `2000 HP`；机枪塔 `20 DPS`、`10` 格射程、`30` 弹药本地库存，每 `1` 弹药支撑累计 `3` 秒有效射击；tracer 只作表现，不持有命中、伤害或耗弹真值；
3. 复用现有城市仓储与物流边界补给塔内弹药。脱网保留余弹并可继续射击直到耗尽，但不能访问城市库存；单塔玩家暂停只冻结该塔，全局战术/系统暂停冻结预警、生成、移动、攻击、索敌、射击、耗弹和伤害；
4. 接入唯一敌人啃噬者：`60 HP`、`1.8 格/秒`、`8 DPS`、攻击距离 `2` 格；第一轮只直达城市核心，不实现建筑/城墙目标选择、阻挡或寻路；
5. 接入首座完成机枪塔触发的唯一教学波：`15` 秒预警、`8` 只啃噬者在 `40` 秒内确定性分批生成；HUD 显示核心生命、预警/波次和存活数，真实输入选择机枪塔/啃噬者可查看生命、目标/距离、射击/攻击、本地弹药及物流状态；
6. 对选敌和表现对象采用稳定目标集、池化和逻辑/表现分层，避免逐帧全场搜索、临时集合分配、`InvokeRepeating` 及逐发创建/销毁；完成聚焦测试、日常完整 EditMode、完整 PlayMode、项目质量门、正式构建、文档生成、验证与 `RecordVerification`。

明确排除：敌人掉落和尸体回收；建筑/城墙寻路、阻挡、受击与维修；其他敌人和炮塔；关注度及后续波次；领袖战斗、碾压和技能；弹丸命中真值；正式失败结算、存档迁移、schema 修改及冻结 2D 新功能接线。

F1C/F1D 中“敌人、炮塔与战斗不在本阶段”的文字继续作为对应历史里程碑边界保留；F1E 只以 `IDEA-0013` 批准的最小切片承接后续，不反向扩大此前已完成范围，也不表示 GDD 的完整战斗系统已经实现。

### 阶段 F1F：IDEA-0014 正式撤离与完整 3D 垂直切片

状态：**已实现待验证**。提交链 `cc660c3` 至 `ef6f10a` 已完成书面设计、正式部署与撤离规则、原子容量事务、生产/研究/防御撤离锁、批次编排、不可变视图、正式输入与场景接线、六段端到端和混合性能门。六段 E2E `1/1`、相关真实输入组合 `35/35`、Task 9 聚焦 `70/70` 已通过；GUI Profiler 在 macOS Unity Editor PlayMode 的 300 帧平均为 `7.3069 ms`，稳定适配器 300 次分配 `0 B`，对象和监听器保持稳定。后续最终收尾又完成日常 EditMode `1787/1787`、完整 PlayMode `112/112`、项目质量门、Windows Release 3D、Windows Development 3D、legacy 2D、macOS universal 3D 四个正式构建及 `RecordVerification`。这些仍是自动化、开发机和跨平台构建证据，不是用户试玩或真实 Windows 验收，因此本阶段不能写成“已验证”。

目标：在复用既有部署、撤离锁、仓储、生产、研究和防御真值的前提下，把 IDEA-0003 的灰盒外城资产处理升级为可在真实敌袭中使用、后果可见、事务安全的正式撤离，并建立一条不绕过正式输入的六段连续验收。

已实现范围与顺序：

1. 先以失败测试固定正式部署与收起规则：展开基础 `5` 秒、收起基础 `8` 秒，实际时长除以城市生产力；转换可取消且已用时间作废；第一名敌人生成至存活数归零为战斗状态，战斗收起的正常规则进度再乘 `70%`；正式按键继续使用 `F`；
2. 增加纯规则撤离策略上下文，不让 UI 或撤离控制器复制战斗、生产率、退款和容量判断；和平完整拆除 `80% + 原施工时间 50%`，战斗完整拆除 `60% + 5 秒`，快速 `50% + 立即`，遗弃 `0% + 废墟`；
3. 把建筑内部输入/输出、塔内弹药、仓库内容和完整退款纳入同一预检及原子提交边界。清单预览随权威战斗状态刷新，确认批次时冻结该批次的战斗处置上下文和正式人口生产力；开发规则时间加速保持独立。仓储内容和容量在每项提交前重新预检。完整/快速拆除在容量不足时整项拒绝并显示缺口、停止批次且不移除建筑，同时保留原批次 work/锁供玩家用 `E` 腾出容量后重检，不得借失败重建和平批次；遗弃明确显示普通建筑内部物资和塔内弹药损失，仓库内容仍不得删除；
4. 保留 `IsEvacuationLocked` 作为生产、研究、仓库和防御共享生命周期门；锁定、失败回滚、提交移除和恢复不得创建第二套建筑资格。撤离清单不自动暂停世界，但允许现有战术暂停；
5. 增加只读撤离 view-model 和正式 3D UI：显示处理、退款、耗时、未完成比例、物资后果、容量失败、稳定队列、当前项目与剩余时间；所有操作通过正式 Input System，不能直接调用内部回调冒充玩家输入；
6. 增加单一真实输入 PlayMode：移动/寻路 → `F` 展开 → `B` 建成研究站 → `T` 依次研究基础冶金、弹药装配和自动防御 → `B` 建生产链和机枪塔 → 真实产弹与耗弹防御 → 敌袭中 `F` 撤离 → 完成 Packing → 返回 Mobile 并再次驾驶；夹具只允许加速时间和提供确定性初始资源；
7. 补活跃生产、八敌、防御 HUD 与撤离 UI 的混合性能门和 GUI Profiler 标记；完成聚焦测试、日常完整 EditMode、完整 PlayMode、项目质量门、四个正式构建、官方文档生成/验证和 `RecordVerification`。

明确排除：正式 3D 存档适配、schema 修改、前哨、迷雾、多城市、跨城运输、新敌人/炮塔/波次、建筑受伤维修、正式失败结算、批量正式美术音频和冻结 2D 新功能。正式存档在本阶段通过后另行设计；若需要 schema `31`，必须先向用户单独申请批准。

### 阶段 F1G：IDEA-0015 正式 3D 存档与 schema 31

状态：**已实现待验证**。schema `31` 正式 3D 单槽存档、自动检查点、安全文件事务、五类活动状态恢复和场景入口已经实现；实现 HEAD `a8f30af` 的日常 EditMode `2069/2069`、完整 PlayMode `127/127`、项目质量门、Windows Release 3D、Windows Development 3D、legacy 2D、macOS universal 3D 四个退役前正式构建、官方文档生成/验证和 `RecordVerification` 均已通过。Task 14 已删除 `FormalPrototype` 与 47 个专属运行时脚本（共 51 个 `MonoBehaviour` 类型），移除 legacy 2D build，当前只保留正式 3D 场景及三类 3D 构建；退役后的聚焦 EditMode `317/317`、日常完整 EditMode `2039/2039`、完整 PlayMode `77/77`、项目质量门、三项构建、官方文档生成/校验和正式验证记录均已通过。用户人工试玩与真实 Windows 10/11 视觉、GPU、显存和内存验收仍未完成，因此不得写成“已验证”。

目标：建立全项目唯一的正式存档 Store/Coordinator，以独立 3D payload 保存当前单城市正式会话，并通过 schema `31` 覆盖世界、城市、建筑、仓储、背包、合成、研究、生产、防御和已确认撤离批次的权威状态；UI、选择、悬停、预览和其他可重建表现不进入存档。

已批准范围与顺序：

1. 先以失败测试固定 schema `31` envelope、`gameVersion`、`saveSchemaVersion`、`contentSources`、`createdAt`、`updatedAt`，以及损坏、未来版本、缺失或重复实例 ID、非法范围与错误数组长度的拒绝边界；未知内容定义 ID 必须保留为缺失内容占位或孤立数据，不能静默删除；
2. 建立显式、单向、不可回改的迁移和正式回归样本。schema `1–30` 是旧 2D payload，必须继续可读并保留原语义，绝不能被静默解释成 3D；若没有用户明确选择的导入流程，3D 入口只报告其为旧 2D 存档；
3. 统一文件入口必须使用临时文件、刷新、原子替换和迁移前备份；主档损坏、写入失败、权限不足或迁移失败时保留原档，并显示可理解错误；
4. 各领域模型继续拥有自己的纯快照和恢复，Store/Coordinator 只编排捕获、验证、迁移和确定性恢复顺序。保存必须覆盖 Mobile、Fortress、Deploying、Packing、教学战斗，以及已经确认并进入处理的撤离批次；未确认的菜单、选择、悬停、蓝图和其他 UI 状态不保存；
5. 先完成纯数据、迁移、文件事务和领域状态测试，再完成正式 3D 场景真实输入的退出保存、重开恢复、损坏回退与可见错误测试；随后运行日常完整 EditMode、完整 PlayMode、项目质量门、四个正式构建、官方文档生成/验证与 `RecordVerification`；
6. 只有 schema `31` 的上述自动化、四构建和验证记录全部通过后，才可在单独可回退步骤中退役 `FormalPrototype`、2D 专属控制器和 legacy 2D 构建入口。退役不得删除共享规则，也不得删除 schema `1–30` decoder、既有单向迁移和当前/上一版本回归样本。

当前实现结果：`formal-world.json` 是唯一正式进度槽；开始新进度、继续、保存并退出和自动检查点共用 schema `31` envelope、codec、validator、Store/Coordinator。检查点由新进度就绪、首次展开、首座机枪塔、教学战斗开始、撤离批次确认、撤离工作提交和收起完成事件触发；不逐帧轮询写盘。写入先生成、刷新和复读校验同目录 `.tmp`，再更新已验证 `.bak` 并原子替换主档；主档损坏可回退备份，未来版本、旧 2D、读写失败和无有效存档均有结构化中文结果。schema `1–30` 继续被识别为 legacy 2D 历史 payload，不会自动恢复进 3D；场景运行时退役不删除其 decoder、固定 fixtures 或中文兼容说明。

当前验证证据：20 次完整 coordinator capture 的 payload hash 稳定；单次完整快照与文件事务分别使用 `1 MiB`、`4 MiB` 明确分配上限；真实输入 PlayMode 分别往返 Deploying、Packing、八敌活动战斗、已确认撤离处理中、容量阻塞撤离。容量阻塞流程连续实际卸载/重载场景 5 次，每次都核对主档和备份的字节、创建/修改时间与属性不变、旧 host 已销毁且新 composition 唯一；最后再经过 300 个真实 PlayMode 帧，确认没有自动写盘或场景对象增长。实现 HEAD `a8f30af` 的日常 EditMode `2069/2069`、完整 PlayMode `127/127`、项目质量门、四构建、官方文档生成/验证和 `RecordVerification` 也已通过。以上自动化与构建证据不替代用户试玩或真实 Windows 10/11 视觉、GPU、显存和内存验收。

验收门：捕获—加载—再次捕获保持权威状态等价，跨保存点继续模拟仍确定；稳定建筑实例 ID 与下一序号不碰撞；活动生产、研究、合成、防御、转换和已确认撤离批次不丢失、不复制资源或重复提交；文件事务失败不损坏主档与备份；旧 2D fixture 继续通过。用户人工试玩、真实 Windows 路径/权限、首次启动和退出保存只能在实际执行后记录，不能由 macOS 自动化或构建成功代替。

### 阶段 F1H：IDEA-0016 四路线资源、科技树与六类透明视觉图

状态：**已实现待验证**。正式 3D 已扩展为 `31` 种资源、`30` 条正式配方和 `43` 节点正式研究，接入自下向上科技树、多输入/多输出与同建筑多配方、派生资源发现、完整合成/资源观察、Development 修改器中文查询，以及六类共 `114` 项正式二维图、统一视觉目录、`30` 条配方视觉投影和六类 SpriteAtlas。实现提交 `41de498031c3eaea086360c55693710defb74cb8` 的完整 EditMode `2187/2187`、完整 PlayMode `84/84`、项目质量门、Windows Release 3D、Windows Development 3D 与 macOS universal 3D 三项现役构建已经通过；用户逐项视觉试玩及真实 Windows 10/11 的视觉、GPU、显存和内存验收仍未完成。

目标：在不修改当前 `64×48` 地图及其资源节点布局、不增加第二套库存/配方/研究真值的前提下，统一四路线使用的资源、材料、配方和研究目录；把正式 3D 科技界面扩展为四路线共存的 `43` 节点底向上树，并为 UI、物品、科技、建筑、人物和世界标记六类内容提供可替换的透明视觉图。四条路线按 GDD 同时存在，不设置互斥选路；跨路线桥节点继续遵循正式前置关系。

批准范围与实施顺序：

1. **目录统一与 TDD**：先写失败测试，审计并统一资源、配方、研究、图标和中文显示目录。稳定资源 ID、科技 ID、配方 ID、建筑 ID 及既有权威所有权保持不变；运行时、UI、开发工具和存档只能读取同一目录，不得各自复制名称、数值、前置或解锁条件。质量目录与可复用项目目录同步纳入新增正式生产文件；
2. **资源与配方闭环**：以 GDD 正式数值扩充四路线需要的材料及生产/合成关系，复用现有城市库存、仓库过滤、背包、建筑内部缓存、物流范围、停工原因和配方时钟；先以规则测试证明输入、输出、容量、断供、暂停、物流断连和恢复，再接合成界面、资源状态栏、建筑详情与存档往返。不得通过改地图或向 seed `8128` 增添节点来伪造供给闭环；
3. **43 节点底向上科技树**：以现有正式稳定 ID 和 GDD 前置关系为真值，把根节点放在界面底部、后续层级向上展开；技术、修仙、生物飞升、灵能四路线同时可见、可检索并保持各自颜色/图例，桥节点清楚显示多路线前置。研究启动、成本、时间、暂停、完成与恢复继续由统一研究模型持有。只有已经接入权威运行时效果的节点可以描述为生效；尚未接入战斗、单位、建筑模型、动画或特效的节点必须明确标为“预览/效果待接入”，不得用解锁文本冒充实际玩法；
4. **开发修改器中文双搜索**：保留仅 Editor/Development Build 可用和数字键 `0` 入口，增加资源与科技两个独立中文搜索列表；主显示使用正式游戏内中文名称，stable ID 只作为内部键。玩家可选中资源后直接增加指定数量，也可选中单项科技后精确解锁；不得绕过正式库存容量和研究会话适配，不得让 F10 恢复为入口或让修改器进入 Release；
5. **逐项内容简介与视觉约束**：在生成任何候选图前，先为计划接入的每项内容给出简短用途说明，并明确所属类别、使用位置、目标显示尺寸、透明背景、轮廓识别、路线/状态色、不可混淆对象和占位回退等约束；这些说明和约束先供用户理解与核对，不能用未经说明的批量生成结果倒推风格规则；
6. **六类各五张代表性样图供用户选择**：分别为 UI、物品、科技、建筑、人物、世界标记制作各 `5` 张透明背景代表性样图，在同类别内使用一致尺寸与展示条件供用户比较。这里是每类 `5` 张代表样图，共 `30` 张，不是每个正式资产制作 `5` 个版本；每一类的风格都必须由用户明确选择或要求调整。自动评分、代码审查、测试截图或开发者主观判断都不能代替用户的分类风格选择。旧“10 张通用透明样图”方案及其任何产物全部废弃，不进入 Git 仓库、Unity 工程、项目质量目录或可复用目录，也不得作为默认选定风格；
7. **类风格确认后的批量正式图接入**：只有相应类别的风格经用户确认后，才按该类别约束批量制作正式透明图；记录来源、许可、生成/编辑方式、稳定键和 Unity 目标路径，通过统一视觉目录接入资源状态栏、背包、合成、仓库、生产详情、世界资源浮标、科技树、建造/建筑界面、人物界面和开发修改器。缺图继续使用清楚且可替换的正式回退，不得因个别缺图阻断运行，也不得把程序化占位写成最终美术；
8. **正式验收与收尾**：逐阶段运行聚焦测试，再运行日常完整 EditMode、完整 PlayMode、项目质量门、真实输入端到端、目录/监听器稳定性和混合性能门；对现有 schema `31` 做新资源、配方、研究完成态与进行中状态的捕获—恢复—继续模拟回归，并完成 Windows Release 3D、Windows Development 3D、macOS universal 3D 三项现役构建、官方文档生成/验证和 `RecordVerification`。只有实际执行过的结果才能写入验证记录。

存档与范围边界：本阶段固定使用 schema `31`，不得增加字段、改变 payload 身份或静默迁移；如果正式闭环确实需要 schema 修改，必须停止实现并另行向用户申请批准。地图尺寸、seed `8128` 内容、资源节点位置、地形真值、寻路和部署规则全部不改。本阶段不新增敌人、炮塔、波次、弹丸、军队、跨城市运输、前哨或迷雾，也不批量制作建筑/单位 3D 模型、动画、VFX、SFX；目录中的相关科技只按上述诚实预览边界显示。

验收门：四路线目录、资源配方闭环和 43 节点投影必须由失败测试起步，并通过同一权威模型、正式 3D UI 与真实 Input System 主循环验证；搜索框聚焦时快捷键不得穿透，失焦后才恢复。性能门必须证明树、视觉图和修改器列表不会引入逐帧全量重建、监听器累积或无界对象增长；schema `31` 往返不得丢失、复制或错误解锁资源与研究。六类都必须先完成逐项内容简介和视觉约束，再各自提交 `5` 张代表性样图；未经用户确认的类别不得进入批量正式图制作。用户的分类风格选择、最终视觉判断和完整试玩结论只能由用户给出，三项构建和自动化不能替代；旧“10 张通用透明样图”及其产物不得进入任何提交或项目目录。

当前实现记录：资源、配方与研究单一真值已经落地，生产状态、模拟、运行时、存档和可观察快照均支持多通道和配方切换；资源状态栏、账本、背包合成、生产详情与修改器消费统一目录。物品、建筑、科技、UI、人物和世界标记正式透明图均已形成正式批次，限定目录导入、稳定 GUID、缩小/Alpha 联络表、共享目录与运行时消费者已经完成对账。完整自动化、项目质量门、三项现役构建及正式验证记录已经通过；用户逐项视觉验收、完整试玩及真实 Windows GPU/显存/内存验收仍未完成。

### 阶段 F1I：IDEA-0017 单城市十波防御战役

状态：**已实现待验证；相关聚焦自动化通过，待人工与最终完整收口**。

目标：在不修改当前地图、不新增三维模型和不建立第二套库存、建筑、科技或地图真值的前提下，把 `IDEA-0013` 的一塔一敌教学竖切扩展为三塔、三敌、十波、建筑受击、玩家可见暂停/倍速、胜负结算和统计闭环。

已完成的实现范围：

1. 先以正式目录固定机枪塔、激光塔、孢子塔，啃噬者、晶壳兽、啸叫者和十波配置；数值只由目录提供，伤害克制只读取既有 `DamageMatrix`；
2. 把机枪塔专用运行状态收敛为定义驱动的通用塔状态，继续复用城市物流库存、塔内耗材、确定性目标和直接伤害；tracer 保持表现，不升级为弹丸真值；
3. 建立建筑生命与公开摧毁边界，让晶壳兽优先城墙、啸叫者优先生产建筑，目标不存在时确定性回退核心；不新增 NavMesh 或复杂封路；
4. 接入显式十波状态机。第一波保留 `15` 秒预警和 `40` 秒生成，后续生成处于 `45–75` 秒；当前波必须在最后一批生成且存活敌人为零后完成；
5. 接入统一暂停、`1×`、`2×` 玩家入口和真实输入优先级；终局只发布一个结算模态，相同 terminal revision 幂等，关闭后释放监听与阻断；
6. 完成会话统计：规则时间、波次、三敌击杀、三塔伤害/击杀、耗材、建筑损失、核心生命、生产批次与有效效率、坚守/机动、部分迁移和修改器使用标记；胜利只允许继续沙盒，失败只允许读取最近波前或返回标题；
7. schema 升级为 `32`，完整往返通用塔、十波战役、建筑生命和统计，并提供 schema `31→32` 的确定性迁移；schema `1–30` 的识别、解码和固定样本继续保留；
8. 失败重试使用独立内部最近波前档，复用正式 codec、validator 与原子文件事务，不形成第二个玩家存档槽；新波前替换旧波前，失败保留旧有效档；
9. 相关规则、DTO、存档、结算 UI、真实输入阻断和稳定性聚焦自动化已经通过。下一步仍须执行日常完整 EditMode、完整 PlayMode、项目质量门、三项现役构建、官方文档生成/验证和 `RecordVerification`，并由用户完成试玩与真实 Windows 复验。

不包含：Boss、结晶掘地者、关注度、掉落/尸体、领袖战斗、军队、前哨、迷雾、多城市、复杂路径阻挡、真实弹丸、新三维模型、动画、VFX、SFX、维修/重建或正式发布。用户于 2026-08-24 批准本阶段优先于前哨、迷雾和领袖交互实施；这是路线顺序调整，不删除后续范围。

### 阶段 F1J：IDEA-0018 文明式地图视觉与正式 3D UI 比例统一

状态：**已实现待验证；自动化、固定 GUI 证据和三项构建通过，等待用户视觉试玩与真实 Windows 复验**。

目标：不改变当前玩法地图、资源、存档和 schema `32`，重制七类正式地表的多观察距离表现，并从 Canvas 根部统一现役正式 3D UI 的尺度、安全区、比例和信息层级。

实施顺序：

1. 以失败测试冻结 `64×48`、seed `8128`、3072 格四字段、资源节点稳定身份、世界坐标、world signature、七层顺序、唯一地形 Presenter 和 Ruins/Cliff 稳定资产合同；
2. 建立共享 UI Layout/Profile/Policy，统一三个 Canvas 为 `1920×1080` 参考分辨率和同一缩放策略，并保护唯一 EventSystem、排序层和现有模态输入优先级；
3. 优先整理顶部资源栏、左上危险 HUD、右侧详情、底部建造栏/缺料反馈和速度控件，使常驻信息不重叠且地图保持最大可视面积；
4. 再整理资源账本、背包/合成、科技树、撤离、系统菜单、结算和开发修改器：面板服从安全区，长内容滚动，不整体压缩正式字号；
5. 补齐 GDD A16.10 的正式滚轮镜头缩放，并把资源节点标记分为近/中/远三档表现；节点类型、数量、储量和采矿合法性仍只读 `WorldMapModel`；
6. 在稳定路径和数组层序下重制 Wasteland、Rocky、Wetland、Crystal、Ruins、DeepWater、Cliff 七类源贴图，扩展同一 Profile/材质/Shader 的低频宏观色区、受控层内色阶和特殊边界可读性；不建立第二套地图，也不复制《文明》资产；
7. 以默认总览、基础层交汇、深水岸线、Ruins/Cliff 接缝、资源标记三档、建造叠加和主要 UI 面板形成固定证据；随后运行聚焦测试、`TerrainAssetDeep`、完整 EditMode、完整 PlayMode、项目质量门、三项现役构建、文档生成/验证和 `RecordVerification`。

当前实现提交 `2d340d8844dae00ff7760ed44709e441ad877d72` 已完成上述生产范围：七类地表四通道从不可变概念源确定性生成，滚轮缩放和矿点 Near/Mid/Far 已接入，三个 Canvas 与主要 HUD/大面板已统一比例，目录/配方真实输入回归已修复。`TerrainAssetDeep` `20/20`、日常 EditMode `2515/2515`、完整 PlayMode `88/88`、项目质量分析、15 张固定截图、10 帧缩放、300 帧水面、Windows Release/Development 和 macOS universal 三项构建均通过。以上只证明开发机自动化与构建，不代表用户已经完成视觉试玩，也不代表真实 Windows 10/11 GPU、显存和内存验收完成。

不包含：地图扩为 `96×64`、改六角格、重新生成资源/布局、迷雾、前哨、多城市、新玩法内容、新三维模型/动画、地形顶点位移、第三方 UI/地图依赖或正式发布。自动化与开发机截图不替代用户视觉试玩和真实 Windows 10/11 GPU、显存、内存验收。

### 阶段 F1K：IDEA-0019 正式地图内容重排与世界表现尺度统一

状态：**已实现待验证**。

目标：保持当前 `64×48`、seed `8128`、方格玩法和 schema `32`，把中央旧图加空白外圈升级为覆盖全图的 v2 连片布局，并统一建筑、炮塔基础座、废墟/预览/选择标记、资源 Marker 和正式 UI 图标比例。

实施顺序：

1. 先以失败测试冻结 v2 地图合同：`3072` 格、宏区连通、出生保护、双通路、特殊地形簇、正式目录固定的 `24` 个精确坐标/资源/储量、GDD 安全铁/石与裂谷铁、真实 `2×2` 采矿锚点和真实非零全图 golden SHA-256；
2. 建立纯 C# 正式 v2 生成配置和生成器：地形由固定 `8×6` 宏格模板、每宏格两个稳定整数偏移通道和一轮清理产生，资源按 `FormalResourceNodeSpec3D` 原样投放，不实现评分候选、定点插值或随机 spot；`GrayboxWorldLayout3D` 只作默认世界 facade，`WorldMapModel` 继续拥有运行时格位、储量、揭示和采收真值；
3. 将 world generation version/signature 升级为 v2，保持 schema `32` codec 不变；自动验证 v2 往返和 v1 地图存档中文拒绝且运行时原子不变；
4. 先以失败测试覆盖 Ground/Inner、1×1/2×2/3×2、四方向、全部建筑类别及 74% footprint、精确 `.14` 格高的 DefenseFoundation，再接纯表现尺度 Profile；逻辑 footprint、旋转、放置、物流和存档坐标不变；
5. 扩展正式地图导航与 UI Profile，以屏幕像素/地面格相对高度驱动 Marker Near/Mid/Far；当前用户比例迭代基线为 Near `68/50/22`、Mid `56/42/20`、Far Icon `28`。UI 图标继续由 `FormalUiLayoutProfile3D` 的语义 token 统一；默认镜头不显示全图名称，普通标签按稳定 ID 确定性避让，建造栏 `620×54` 与反馈位置不变；
6. 更新地表、Ruins/Cliff、场景出生与固定证据消费 v2 真值，保留唯一地形 Presenter、14 个稳定 Prefab、两类合批、13 个共享材质和分类原子回退；
7. 运行地图/放置/存档/比例/真实输入聚焦测试、`TerrainAssetDeep`、日常完整 EditMode、完整 PlayMode、项目质量门、三项现役构建、固定 GUI 证据、官方文档生成/校验和 `RecordVerification`。

不包含：`96×64` 扩图、六角格、迷雾、区块流送、随机多世界、新资源/建筑/模型/动画、复杂 NavMesh、跨城市运输、UI Toolkit 重写、v1 地图存档迁移或正式发布。用户视觉试玩与真实 Windows 10/11 GPU、显存、内存验收继续由实际结果确认。

实现已经形成 v2 固定宏格/整数扰动、固定 24 节点、generation/signature、建筑尺度 Profile 和 Marker/UI 比例接线，并以 SHA-256 `2f0ecd374ad3a1bf6fd50564d949741618c7ce1b72bc6619f67acda632b1e6fd` 冻结 3072 格 canonical 内容；场景双 Profile、标签避让、完整回归、项目质量门和三项现役构建均已通过并记录。用户对地图、矿区、标签和比例的最终视觉试玩，以及真实 Windows 10/11 GPU、显存和内存验收仍未完成。

### 阶段 F2：跨路线桥节点

状态：**`IDEA-0021` 已实现待验证**。文明 Lv.2 自然开放合金装甲和御剑术，并已闭合两条原位升级命令。

目标：把六个桥节点从研究文本升级为可观察、可制造、可存档的玩法，再为实现完成时 `BuildingCatalog.All` 的全部建筑统一生成透明贴图，并将新树枝科技树 UI 设计落到正式 3D。

1. 灵能机甲厂；
2. 高周波飞剑铸造台；
3. 生物机库与半机械巨兽；
4. 灵植园；
5. EMP 塔与机械敌人瘫痪；
6. 血肉灵丹的三倍效果和突变风险。

桥节点不得复制第二套生产或战斗系统，必须复用现有资源库存、建筑运行时、友军控制器、伤害矩阵和存档入口。

本轮可执行边界：最多新增灵能机甲厂、高周波飞剑铸造台、生物机库、灵植园和 EMP 塔五座建筑；血肉灵丹复用既有炼丹房。精确配方和效果由 `IDEA-0021` 设计规格冻结。美术覆盖现有 30 座与最多 5 座新建筑；新图必须经统一视觉目录、Atlas、建造卡、选中详情与世界表现真实接线。新科技树保留实际目录、状态和 Input System 真值，只更新布局与视觉层。地图、seed、节点、地形、schema `33` 和现有 3D 模型/占位保持不变。

### 阶段 F3：军队与远征

状态：**`IDEA-0022` 已实现待验证**。一个 12 单位稳定小队、四类配置单位、制造/维护休眠、守卫/跟随/出征/撤退、领袖带队和确定性远征/返城战利品已接入正式 3D；自动化通过，用户试玩待完成，不做逐单位微操。

目标：从自动护城单位扩展到玩家可理解、可指挥的军队系统。

- 军队面板、单位分类、上限和维护成本；
- 集结点、守卫/跟随/出征/撤退命令，以小队为最小玩家指挥粒度；
- 领袖带队士气加成；
- 远征结算、伤亡、战利品和撤退；
- 存档与加载后的命令恢复；
- 占位选择框、路径线和状态图标。

本阶段先做单城市军队，不提前实现多城市共享军队。

### 阶段 F4：多城市与正式世界层

状态：**`IDEA-0022` F4A 已实现待验证**。主城引用、一次城、一显式前哨、独立库存/自治、查看/控制权、30 秒领袖调动和实体 Convoy 装载/在途/护送/外交免疫/卸载已形成闭环；原 64×48 地图、seed 与 24 节点未重做。

目标：实现主 GDD 正式版的多城市决策价值。

- 城市实例 ID 与城市存档模块化；
- 多城市总览和焦点切换；
- 已通信城市允许自由查看；直接控制前期需要领袖到场，远程指挥科技解锁后才允许跨城接管；
- 城市、前哨和生产区之间的实体资源运输、护送、拦截与风险；
- 前哨库存、补给、能源、维护、通信、委派角色、自动防御和分级受袭通知；
- 多城市关注度、危机轨道和监察者打击；
- 集体意识共享研究的真实效果；
- 世界地图节点与城市迁移。

进入本阶段前必须先完成存档 schema 迁移设计和旧单城市存档回归样本。

### 阶段 F5：外交、继承与长期文明

状态：**`IDEA-0022` F5A 已实现待验证**。三角色、倒地/城市医疗救援/恢复伤势/死亡遗体、临时议会、指定继承人/低支持政变、三内部派系和两外部势力的接触/三类报价/交易/协议状态已接入；长期高阶政治仍保留未实现。

目标：实现长期正式版的政治和叙事层。

- 势力关系、外交选项与代价；
- 领袖倒地时限、角色或城市救援、重伤、永久伤势、遗体和装备回收；
- 临时议会、指定继承人、竞争候选、紧急选举和路线/制度继承方式；
- 忠诚、派系、政变、内战、城市归属与新领袖长期路线影响；
- 意识备份等路线能力对正常死亡流程的有代价改写；
- 文明等级 3 以上的条件、奖励和危机；
- 三派监察者叙事与长期结局；
- 跨循环继承与多存档管理。

该阶段不得早于 F4 的数据和存档稳定门。

### 阶段 F6：正式 3D 表现、可用性与候选版

状态：**`IDEA-0023` 图像可完成子阶段已实现待验证**。本子阶段保持 schema `34`、现有地图和玩法不变；20 张透明图已通过 140 项统一目录和七 Atlas 接入，四单位进入军队图像条与主小队公告板，次城/前哨/运输队进入垂直公告板，三角色进入政务头像，M/N/P 使用正式框体、页签和八个运行时状态徽记。真实三维建模、动画、音频、复杂特效、完整设置/无障碍和候选版归档仍未进入，贴图替代不得写成模型已完成。

目标：用正式资源替换占位并形成可长期试玩的内部候选版本。

- UI 信息架构、完整设置、键位和可访问性；F1B 的 `IDEA-0007` 只提前实现分辨率、窗口/全屏、操作说明和退出入口，音乐、音效、震动、键位重映射、手柄与完整无障碍仍留在本阶段随实际消费系统接入；
- 基于 F1B 已验证接口批量替换城市、建筑、单位、敌人、地形、特效、音频和音乐资源；
- 统一比例、锚点、排序层、碰撞代理和动画事件；
- 首次引导、科技说明、战斗反馈和失败解释；
- 性能预算、长局稳定性、存档回归和 Windows 兼容；
- 人工试玩、问题分级、版本说明和候选包归档。

`IDEA-0023` 的贴图子阶段执行边界：新图必须通过稳定视觉 ID 和统一目录接入，世界对象的比例、锚点和位置偏移由表现配置负责；UGUI 文本与状态保持实时数据驱动，不把名称、数值或状态烘焙进图片；旧程序化表现只保留为缺图回退。详细资产范围和验收条件见 `Docs/06-User-Feedback-and-Change-Control-ZH.md` 的 `IDEA-0023`。

`IDEA-0024` 状态：**开发中**。该子阶段按用户图二重制科技树表现，补 Development 启动验收入口、可点击 M/N/P 页签、建筑 Sprite 可见性和移动城市/8×6 甲板比例；继续保持 44 科技、材料图标、地图、玩法、旧物理边界和 schema `34` 不变。详细范围见 `Docs/06-User-Feedback-and-Change-Control-ZH.md` 的 `IDEA-0024`。

## 4. 固定质量门

每个功能里程碑必须同时满足：

1. 开始前 `git status`，不覆盖用户改动；
2. 规则测试先于运行时实现；
3. 纯规则优先为普通 C#，MonoBehaviour 只适配输入、场景、物理和显示；
4. 不删除测试、不降低断言、不忽略异常；
5. 全部 EditMode 测试通过；
6. 全部 PlayMode 测试通过；
7. Unity 无界面编译通过；
8. Windows 构建成功；
9. 独立程序至少 12 秒冒烟，0 运行时异常；
10. `git diff` 只包含当前里程碑；
11. 提交后工作区干净；
12. 新表现提供稳定的正式美术替换槽；
13. 3D 迁移不得删除 2D 回归基线，并必须保留可关闭或移除新灰盒场景的回退路径。
14. `DOC-0001` 质量基础设施：每次功能修改都要更新目录映射和对应测试，生成技术附录后做只读验证；生成器不改变玩法审批、人工试玩或验收结论。

这项自动化只帮助发现目录、文档和测试证据是否遗漏，不计入玩法完成度，也不提高主 GDD 或美术完成百分比。

## 5. GitHub 与保密要求

- GitHub 仓库必须设置为 **Private**；
- 当前不创建公开 Release、Pages、公开 Issue 模板或商店页面；
- Unity 工程与本文档目录放在同一个仓库；
- `Library/`、`Temp/`、`Logs/`、`Builds/`、`TestResults/` 和本地存档不提交；
- 大型二进制资源继续由 Git LFS 管理；
- 不提交账号令牌、机器凭据、个人存档或许可证文件；
- 正式美术加入后按资产批次提交，并保留来源与许可证记录。

## 6. 下一实施顺序

完成本路线图归档后，按以下顺序继续：

默认编辑/构建入口切换已由独立提交 `8ce573a` 完成；Task 14 又完成 2D 专属场景、适配器和构建入口退役实现。共享纯规则与 schema `1–30` 兼容回归继续保留。后续顺序为：

1. 收尾 `IDEA-0004` 的 Ruins/Cliff 运行时子项：实现已经完成，下一门是用户在默认 3D 场景复验比例、轮廓、接缝、遮挡和分类回退，并在真实 Windows 10/11 Player 补 GPU、显存与视觉冒烟；若复验暴露视觉问题，只沿已批准 Catalog/Profile、14 个 Prefab、两类合批和分类回退边界修正，不修改地形玩法真值、schema `30` 或冻结 2D；
2. `IDEA-0007` 至 `IDEA-0010` 3D 易用性跟进包已按住房规则对账 → 外城真实范围覆盖层 → 采矿兼容节点及合法锚点高亮 → 模态退出与首批设置菜单的顺序完成，并于 2026-08-14 通过用户人工试玩，不再占用后续实施序列；
3. 对已完成的 `IDEA-0005` 研究站移动建造子范围与 `IDEA-0006` `64×48` 地图补用户试玩和真实 Windows 10/11 冒烟；研究 UI 和 `100%/50%` 推进运行时已转入 `IDEA-0011`，首版仍不制作研究队列；
4. `IDEA-0003` 的实施计划、3D 建造选择、28 项目录、旋转、外城/内城双网格、施工、收起前撤离处理、开发修改器和 Task 12 机器验证均已完成，状态为“已实现待验证”；`BUG-0001` 仍决定不修复，新 3D 功能不得记作该问题的修复；
5. `IDEA-0004` 的第一版正式连续地表与 Ruins/Cliff 几何已经按书面规格接入并通过各自自动化与性能门；连续地表严格视觉门仍为失败，但用户接受现状进入第一版并要求记录 DeepWater 动态不足，Ruins/Cliff 则是“已实现待用户视觉复验”。复验收尾后，再评估修正连续地表已知表现问题、城市/建筑样板或 UI 图标接入；不得把有限地形样板完成等同于整个第一版美术包完成；
6. `IDEA-0011` 已完成自动验收并进入用户试玩；当前先完成用户据此批准的 `BUG-0006` 与 `IDEA-0012`，闭环旋转预览、真实仓库、石料节点、统一资源图标和精确缺料反馈；本项不得夹带敌人、炮塔、弹丸或 schema 修改；
7. `IDEA-0013` 首个 3D 防御闭环已经完成自动验收并进入“已实现待验证”；等待用户人工试玩与真实 Windows 复验，不将其写成已验证；
8. `IDEA-0014` 已完成正式撤离、混合性能复验、“移动→展开→建造→生产→防御→撤离”完整垂直切片、日常完整 EditMode `1787/1787`、完整 PlayMode `112/112`、项目质量门、四构建与验证记录，当前仍因用户试玩和真实 Windows 验收未完成而保持“已实现待验证”；
9. `IDEA-0015` 的统一 Store/Coordinator、独立 3D payload、schema `31`、自动检查点、安全文件事务、五类活动状态往返和性能/稳定性门已实现；实现 HEAD `a8f30af` 的日常 EditMode `2069/2069`、完整 PlayMode `127/127`、项目质量门、四个退役前构建、官方文档生成/验证和 `RecordVerification` 已通过，当前仍等待用户人工试玩与真实 Windows 10/11 视觉、GPU、显存和内存验收；
10. Task 14 已退役 2D 专属场景、47 个专属运行时脚本（共 51 个 `MonoBehaviour` 类型）和 legacy build；当前只保留正式 3D 场景及 Windows Release 3D、Windows Development 3D、macOS universal 3D 三类构建。共享规则以及 schema `1–30` decoder、迁移和 fixture 已保留；聚焦 EditMode `317/317`、日常完整 EditMode `2039/2039`、完整 PlayMode `77/77`、项目质量门、三项构建、官方文档生成/校验和正式验证记录均已通过；
11. F1H / `IDEA-0016` 的四路线资源配方闭环、43 节点底向上科技树、开发修改器中文双搜索、统一视觉目录、30 条配方视觉投影、六类 SpriteAtlas 和 `114` 项正式透明图已经完成；完整 EditMode `2187/2187`、完整 PlayMode `84/84`、项目质量门和三项现役构建均通过。地图和 schema `31` 保持不变，正式资产逐项视觉判断、用户试玩与真实 Windows 验收仍待完成；
12. 收口 F1I / `IDEA-0017`：三塔、三敌、十波、建筑受击、统一暂停/倍速、胜负结算、完整会话统计、内部波前重试及 schema `32` 兼容迁移已经实现并通过相关聚焦自动化；继续完成完整回归、性能、三项构建、正式验证记录、用户试玩和真实 Windows 复验。本阶段没有新增建模、改变地图或接入退役 2D；
13. 补齐前哨、迷雾、领袖交互和其余未完成 F1A 功能；
14. 垂直切片通过独立验收后，才开始剩余正式模型、美术、动画与特效批量制作；
15. 六个跨路线桥节点；
16. 军队与远征；
17. 多城市；
18. 外交、继承和长期文明。

每完成一项都更新本文件的基线提交、测试数量、存档 schema 和完成度估算。
