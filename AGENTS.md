# WasteCity 开发者必读规则

本文件适用于整个仓库。所有人类开发者和 AI 开发代理在分析、设计、计划、编码、测试或修改文档前，必须先阅读：

`Docs/06-User-Feedback-and-Change-Control-ZH.md`

## 开发启动门

开始任何任务前必须：

1. 检查补充文档中是否存在与当前任务相关的 `已批准`、`开发中` 或 `已实现待验证` 记录；
2. 在设计、计划、提交信息或交付说明中引用相关稳定 ID；
3. 不得实现审批状态为 `待确认`、`已拒绝` 或 `已撤回` 的新构思和文档变更；
4. 不得把 `已批准`、`开发中` 或 `已实现待验证` 描述成 `已验证`；
5. 若批准内容改变主 GDD 或路线图，先更新正式文档并建立双向引用，再修改代码；
6. 完成开发后回写实现状态、关联提交和验证证据。

## 用户反馈处理

用户反馈测试 Bug、提出新构思或要求改变原文档时：

1. 先更新 `Docs/06-User-Feedback-and-Change-Control-ZH.md`；
2. 分配 `BUG-`、`IDEA-` 或 `DOC-` 稳定编号；
3. 新记录默认标记为 `待确认 + 未实现`；
4. 只有用户明确批准后才能进入设计和开发；
5. 每次新增记录或改变状态都应形成独立 Git 提交并推送当前开发分支；
6. GitHub 暂时不可用时先保留本地提交，连接恢复后按顺序推送，不改写历史。

## 仓库安全

- 不强制推送；
- 未经用户明确授权，不合并默认分支、不创建公开 Release；
- 不提交本机缓存、临时构建、账号凭据、Unity 许可证或机器专用绝对路径；
- 工作区存在无关改动时只暂存当前任务文件，不覆盖或清理他人改动。
